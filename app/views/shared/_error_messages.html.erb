<% if @user.errors.any? %>
  <div id="error_explanation">
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    <!-- エラーメッセージをeachで一つずつ抽出 -->
    </ul>
  </div>
<% end %>