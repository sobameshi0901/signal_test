<div class="form userForm">
  <%= form_for(:session, url:login_path) do |f| %>
  <!-- 新規登録と同じくform_forだが、sessionモデルは存在しないので、リソースの名前とそれに対応するURLを指定 -->
    <p class="explain">メールアドレスとパスワードを入力し、<br>ログインボタンを押下してください。</p>
    <div class="inputForm clearfix">
      <%= f.label :email, "メールアドレス："%>
      <%= f.text_field :email %>
    </div>
    <div class="inputForm clearfix">
      <%= f.label :password, "パスワード：" %>
      <%= f.password_field :password %>
    </div>
    <%= f.submit "ログイン",class: "sendForm" %>
    <%= link_to "新規登録はこちらから", new_user_path, class: "linkButton" %>
  <% end %>
</div>
